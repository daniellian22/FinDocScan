rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    match /extracted_text/{userId} {
      allow read, write: if request.auth != null;  // Require authentication
    }
  }
}